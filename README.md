ansible-role-zeek
=========

An ansible role that will install and configure Zeek. Zeek is an open-source IDS tool that will

Requirements/Dependencies
------------

There are no requirements or dependencies. The role is built to install all modules or dependencies that zeek requires.

Example Playbook
----------------

    - hosts: all
      roles:
         - ansible-role-zeek
